BSL code review checklist:

Naming: ПаскальныйРегистр для процедур, параметров, переменных. Содержательные имена, без префиксов.
Annotations: &НаСервереБезКонтекста для серверных вызовов из форм. Минимум логики на клиенте.
Queries: параметризация через &Параметр (не конкатенация). Нет ВЫБРАТЬ *. Нет запросов в цикле (N+1). ПОДОБНО только с индексом.
Transactions: НачатьТранзакцию в try/catch. ОтменитьТранзакцию первой строкой в catch. Нет интерактива в транзакции.
BSP: ОбщегоНазначения.* вместо прямого доступа. ДлительныеОперации для фона. УправлениеПечатью для печати.
Strings: НСтр() для локализуемых строк. СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку() для шаблонов.
Exports: комментарий-описание обязателен для export процедур/функций.