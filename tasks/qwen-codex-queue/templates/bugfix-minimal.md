# {{TITLE}}

Goal:

- Fix the specific bug described below with the smallest safe code change.

Context:

- Affected area: {{AREA}}
- Symptom: {{SYMPTOM}}
- Expected behavior: {{EXPECTED_BEHAVIOR}}

Constraints:

- Keep scope minimal.
- Do not refactor unrelated code.
- Preserve existing contracts unless the task explicitly says otherwise.

Done when:

- The bug is fixed.
- The change is limited to the necessary files.
- No new regressions are introduced in the touched path.

Verification:

- Run: `{{VERIFICATION_COMMAND}}`
